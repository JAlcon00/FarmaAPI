# Configuración de Test - MySQL en Docker (localhost:3307)
spring.datasource.url=jdbc:mysql://localhost:3307/farmacontrol?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# HikariCP
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=10000

# JPA (sin uso real, pero evita warnings)
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# JWT Test Configuration
jwt.secret=MiFarmaControlSecretKeyParaJWT2025DebeSerLargaYSegura256BitsMinimo
jwt.expiration=86400000

# Logging
logging.level.root=WARN
logging.level.com.farmacontrol=INFO
logging.level.security=DEBUG
